INSERT INTO tab_name [(sp_namen_1 [, ...])]
VALUES (wert1 [, ...]);

UPDATE tab_name
SET
	sp_name_1 = neuer_wert_1
	[, ...]
[WHERE <bedingung(en)>];

DELETE FROM tab_name
[WHERE <bedingung(en)>];


<bedingung(en)> := -- siehe SELECT.sql